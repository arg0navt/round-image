export default class RoundImage {
  static init(img) {
    return new this(img).getDataImage();
  }

  constructor(img) {
    this.info = {
      img,
    }
  }

  getDataImage = () => {
    const cvs = document.createElement('canvas');
    const ctx = cvs.getContext("2d");
    this.info.img.onload = (e) => {
      cvs.width = this.info.img.width;
      cvs.height = this.info.img.height;
      ctx.drawImage(this.info.img, 0 ,0);
      const theImgData = ctx.getImageData(0, 0, cvs.width, cvs.height);
      console.log(theImgData);
    };
  };

  getArrayBuffer = () => this.info.buffer = new ArrayBuffer(this.info.base64.length * 2)
}